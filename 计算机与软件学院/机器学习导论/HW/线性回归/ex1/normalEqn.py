import numpy as np


def normalEqn(X, y):
    """
    正规方程求解
    :param X:
    :param y:
    :return: 最优解,注意返回的是列向量
    """
    theta = np.zeros((X.shape[1], 1))
    pass
    # ================ YOUR CODE HERE ================
    #说明：利用正规方程组求解，并将结果放入theta。
    #提示python中np.linalg.inv()用来矩阵逆

    return theta
